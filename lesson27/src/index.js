document.addEventListener('DOMContentLoaded', () => {
  'use strict';

const calcForm = require('./calcForm');
const ourTeam = require('./ourTeam');
const formHandler = require('./sendFormAjax');
const siteSlider = require('./siteSlider');
const siteTimer = require('./siteTimer');
const tabs = require('./tabs');
const toggleMenu = require('./toggleMenu');
const togglePopup = require('./togglePopup');

// formHandler();
// tabs();
// toggleMenu();
// togglePopup();
// calcForm()
});
